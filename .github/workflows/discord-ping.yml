name: Admin Hourly Coverage ✨

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord hourly admin ping
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          HOUR=$(TZ=America/New_York date +%H)
          MESSAGE=""

          case "$HOUR" in
            08)
              MESSAGE="🌸 **Admin Coverage (8–9 ET)** 🌸
🛡️ VP on Duty: Dev
👤 Active Admin: Dreamy"
              ;;
            09)
              MESSAGE="☁️ **Admin Coverage (9–10 ET)** ☁️
🛡️ VP on Duty: Dev
👤 Active Admin: Kat"
              ;;
            10)
              MESSAGE="🌈 **Admin Coverage (10–11 ET)** 🌈
🛡️ VP on Duty: Dev
👤 Active Admin: Raina"
              ;;
            13)
              MESSAGE="🌼 **Admin Coverage (1–2 ET)** 🌼
🛡️ VP on Duty: Rae
🧪 Active Admin (Trial): Yousra"
              ;;
            15)
              MESSAGE="🍃 **Admin Coverage (3–4 ET)** 🍃
🛡️ VP on Duty: Dev
🧪 Active Admin (Trial): Rishi"
              ;;
            *)
              exit 0
              ;;
          esac

          curl -H "Content-Type: application/json" \
          -d "{\"content\":\"$MESSAGE\"}" \
          $DISCORD_WEBHOOK
